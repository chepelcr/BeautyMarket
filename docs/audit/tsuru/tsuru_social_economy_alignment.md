# Tsuru Ecosystem — Social Economy Alignment (Phase 9)

**Scope:** Reconciles the Social Solidarity Economy (SSE) principles claimed by the landing (`landing-client/src/content/*.json`) and the strategic research (`docs/Rebranding Web_ Economía Indígena Solidaria.txt` — Bribri-grounded principles) against the **actual platform mechanics** verified in Phases 2–7 (markets-api, POS, cross-app-be, jbiller, data-api, storefronts, landing).
**Method:** Principle-by-principle audit. "Supported today?" refers to *product mechanics in code*, not copy. Status vocabulary: **Mechanics** (embodied in working code) · **Partial** · **Marketing-only** (exists solely as landing copy) · **Absent** · **Contradicted** (implementation actively violates the principle).
**Inputs:** `tsuru_landing_audit.md`, `tsuru_capability_inventory.md`, `tsuru_system_discovery.md`, `tsuru_domain_model.md`, `tsuru_user_journeys.md`, `tsuru_architecture_audit.md`. Absence claims re-verified by grep (`trueque|barter|feria|fair_|cooperativ` → zero hits in `server/src`; `trueque|barter|exchange_offer|mutual` → zero hits in `cross-app-be/app`).

---

## 1. Principle Alignment Matrix

Principles merged from three sources: the landing's declared value spine (`community.json` → Comercio justo, Producción local, Consumo consciente, Transparencia, Colaboración, Intercambio), the landing's commercial promises (free / no hidden fees, fairs, mutual networks, cultural preservation), and the research's Bribri-derived principles (reciprocity, mutual aid/Jala de Piedra, Kökö anti-monopoly circulation, Sibö structural protection, Iriria stewardship, matrilineal leadership, economy-as-healing, woven interdependence, language-as-economy).

| # | Principle | Supported today? | Evidence (code/feature or absence) | Gap |
|---|---|---|---|---|
| 1 | **Reciprocity / Intercambio / Trueque (barter)** — moneyless exchange of goods/skills/time (research "reciprocidad sagrada"; `community.json` barter, presented in present tense) | **Marketing-only** | Landing sells a working barter system (`community.json` → `barter`, 3-step flow; `blog.json` article4 "Trueque 101 … en la plataforma"). Zero barter/exchange entity, endpoint, or UI in any of the 7 systems — grep `trueque|barter` over `server/src` and `cross-app-be/app`: no hits; capability inventory §7 confirms ecosystem-wide absence. ToS even disclaims it: platform "facilitador" only (`terms.json` §4/§6). | The flagship differentiator is 100% unimplemented. Needs a barter domain: exchange-offer entity, matching/discovery, agreement record, optional mutual-credit ledger. |
| 2 | **Fairs (ferias) / woven interdependence** — multi-seller shared events; sellers as nodes in an interdependent network, not isolated islands (research "interdependencia tejida"; `fairs.json`) | **Marketing-only** | `fairs.json` describes 3 fair types + a registration flow ("Inscríbete en una feria… solicita participar como vendedor"); `Ferias.tsx` renders an "Active fairs" badge. No fair/event entity or endpoint anywhere (capability inventory §7). Fair CTAs route to the generic register URL (`branding.json` adminRegisterUrl → admin.j-markets.jcampos.dev, itself the legacy app per `tsuru_user_journeys.md`). Architecture is strictly **one org per subdomain** with no cross-org surface. | No event domain, no fair participation/application, no time-boxed aggregated catalog. Prerequisite: a cross-org public catalog (see #3). |
| 3 | **Cooperation & anti-monopoly value circulation (Kökö)** — value flows across organizations; cross-org bundles; collaboration mapped, not competition | **Absent** | No marketplace aggregation endpoint in any repo; no cross-store discovery (`tsuru_capability_inventory.md` §7 "Multi-seller shared marketplace — Missing-but-promised"). Even single-org product browsing is broken: storefronts call `/api/public/organizations/{id}/products|categories` which **do not exist** in `server/src/controllers/PublicOrgController.ts` (verified Phase 4). No bundle/basket entity spanning orgs. | "Canastas comunitarias" (cross-org bundles), seller-collaboration graph, and buyer-side multi-producer discovery all require new domains — and first require fixing the broken single-org catalog loop. |
| 4 | **Mutual aid / collective effort (Jala de Piedra)** — mutual support networks, collaborators/mentors; crowdfunding for cooperative capital | **Marketing-only** | `community.json` → `mutual` ("Encuentra colaboradores, mentores y clientes"). No social-graph, directory, messaging, mentorship, or crowdfunding entity in any schema (capability inventory §7: no buyer accounts, reviews, follows, or community entities). | Entirely greenfield. Research's concrete proposal (Jala de Piedra crowdfunding with micro-contributions) maps to a new funding domain; jbiller's SNS/SQS FIFO pipeline pattern is reusable plumbing. |
| 5 | **Community over individual / community ownership & governance** — democratic decision-making, equitable distribution | **Absent** | Ownership model is strictly single-owner: `organizations.ownerId` (`server/src/entities/Organization.ts`); membership is user→role link only (`server/src/entities/OrganizationMember.ts` → `roleId`). No voting, proposals, co-op org type, profit-sharing, or any governance entity. Worse, cross-app-be auto-creates orgs with `owner_id='system'` from parsed Excel (`app/services/order_service.py::_sync_organization`) — tenant identity created by a batch job, not a community. | "Cooperative" organization type (multi-owner / weighted membership), member voting on org decisions, and equitable-distribution accounting do not exist even as schema. |
| 6 | **Fair trade enforced by system rules (Sibö as protective architect)** — "Garantizamos condiciones justas…" (`landing.json` values[0], strongest verb on the site) | **Contradicted** | The system's rules currently protect no one: markets-api has zero in-app auth and the documented userId↔JWT-sub gateway check does not exist (`server/src/routes.ts`; `api-gateway/template.yml` — verified absent, Phase 6/7) → ecosystem-wide IDOR; RBAC is fully modeled/seeded but **never mounted** (`server/src/middleware/permissions.ts` referenced only in `dependency_injection.ts`); cross-app-be manager gate defaults `is_manager=True`; jbiller stores P12 signing certs + PINs + ATV passwords **plaintext in Postgres** reachable cross-tenant (`organization-configurations/src/models/organization_configuration.py`); Postgres RLS is permissive `using true`. ToS softens the guarantee to "facilitador"/"as is" (`terms.json` §4/§6). | The platform is architecturally the *opposite* of Sibö: it exposes its weakest users (small CR taxpayers) to credential theft and cross-tenant access. Fairness-by-rules starts with mounting the already-built authorization. |
| 7 | **Transparency** ("Sin cargos ocultos, sin sorpresas" — `landing.json` values[3]; trust pill repeated 3×) | **Contradicted** | Contact form fakes delivery — 1s `setTimeout` → success toast, `settings.json contact.delivery='none'` (`landing-client/src/pages/Contact.tsx:24-38`) against a 24h-response promise; CMS publish reports success for a simulated pipeline (`server/src/services/DeploymentService.ts:58-88` uploads config.json, instantly marks success, no build); traction ("miles de emprendedores") and testimonials ("200 clientes fieles") are unverifiable placeholders; phone is literal "+506 XXXX-XXXX"; claimed GDPR cookie banner doesn't exist in the codebase (`cookies.json` vs `src/components/`). | Positive note: pricing transparency is trivially true today — there is **no monetization code at all** (no commission/fee/donation field in any `server/src/entities/*`; Stripe is schema-only: `Organization.ts:26-28` `plan`/`stripeCustomerId`, no SDK). Fix = make stated behaviors real, retire fake successes, publish honest numbers. |
| 8 | **Free access / no hidden fees** (4+ "gratis" CTAs) | **Mechanics (by omission)** | True in practice: zero billing/commission code anywhere (grep `commission|donation|fee|revenue_share` in `server/src/entities` → no hits); `plan` defaults `"free"`; Stripe scaffolded only. POS, e-invoicing, CMS all usable without payment. | Honored by absence, not design — economically unsustainable. Any future pricing collides head-on with promises #8–#9 of the landing audit. Needs an explicit, published solidarity fee model before monetization. |
| 9 | **Producción local / local development / arraigo territorial (Iriria)** | **Partial — strongest genuine alignment** | The stack is deeply localized for Costa Rican small producers: full Hacienda v4.4 e-invoicing chain (jbiller `sales_pipeline.py` — the ecosystem's only mature end-to-end capability), CR fiscal catalogs incl. CABYS (data-api, 34 services), CR geographic hierarchy from official `Codificacionubicacion_V4.4.xlsx`, SINPE Móvil payment bucket (`useCartFlow.ts` payment type '06'), WhatsApp-first checkout (`checkout-modal.tsx`), offline-first POS for low-connectivity contexts (`src/lib/db.ts` Dexie + `public/sw.js` background sync). | This is local *compliance and channel* fit, not local *economy* features: no producer provenance, no "local" discovery, no distance/origin signals, no environmental stewardship data. Multi-country schema exists but only CR seeded (data-api `country_code`). |
| 10 | **Consumo consciente / environmental stewardship** | **Marketing-only** | Value stated in `landing.json`/`community.json`/`about.json`. No product provenance, sustainability attributes, certification, origin story, or producer-bio fields in any product model (`cross-app-be/app/models/product.py` is fiscal: CABYS/tax columns; markets-api template products are demo content). | Needs provenance/story fields on products + display in storefront templates; research proposes inline cultural/origin education at purchase time. |
| 11 | **Colaboración (intra-community teamwork)** | **Partial — real mechanics** | The one genuinely collective mechanic that works: multi-user organizations with email invitations and token-based accept (`server/src/services/MembershipService.ts`, `InvitationService.ts`, public `/api/invitations/token/:token`, `/accept/:token` in `routes.ts`); roles UI in POS `MembersPage.tsx`; cashier/supervisor assignments and manager-approved cash closings modeled in cross-app-be (`app/models/assignment.py`, `closing.py` with DB-computed reconciliation). | Collaboration journeys are degraded: invitation redirect context is dropped at login (must re-click email link), "Decline" is a no-op, the 'cajero' role routes to a nonexistent `/pos` (blank screen), ClosingFlow.tsx is unmounted dead code, and the roles the UI administers are enforced by no backend (`tsuru_user_journeys.md`). Inter-org collaboration: none. |
| 12 | **Matrilineal / women-led empowerment** (research: foreground women-led cooperatives, protect/compensate women's work) | **Absent** | No organization attribute, badge, verification, discovery filter, or content field anywhere references women-led/cooperative status; no photography/iconography system exists at all (landing `branding.json` has empty logoUrl — brand is typographic only). | Greenfield: org-profile attributes (women-led, cooperative, indigenous), curation/visibility mechanics, and brand imagery would all be new. |
| 13 | **Cultural preservation & documentation** ("Preserva y documenta la cultura de ferias comunitarias" — `about.json`) | **Marketing-only** | Preservation exists only as the landing's own origin-story copy (TCU "Comer Orgánico", Feria del Trueque Verde Manantial) and 6 static blog articles (`blog.json`, git-as-CMS). No documentation/archive/story feature in the product; the org CMS (`Page/PageSection/SectionContent`) is generic storefront content with no cultural/story component type in `seeds/component-seed.ts`. | The CMS component catalog (`/api/components`, `entities/Component.ts`) is an existing extension point: add story/heritage section types; a fair-documentation archive would be a new domain. |
| 14 | **Economy as healing / anti-extractive ethos** | **Marketing-only** | Tone exists in copy ("una economía que pone a las personas primero"). Mechanics are neutral-to-extractive-pattern-free mostly by absence (no ads, no conversion-hacking, no dark patterns — but also no features). One counter-signal: POS logs full API response payloads to console in production (`templates/pos-system/src/lib/api.ts`) and registration stashes plaintext passwords in sessionStorage (`Register.tsx:139-145`) — careless with the very users the brand vows to protect. | Healing framing requires the protective/fairness mechanics of #6 plus the community domains of #1–#5; no shortcut exists. |
| 15 | **Language creates the economy (solidarity micro-copy)** | **Absent in product, present in landing** | Landing copy is saturated with SSE vocabulary (Phase 1 census). The actual product surfaces use standard e-commerce/POS language: storefront cart/checkout ("carrito", WhatsApp order), POS "Clientes" (`useClients.ts`, ClientDrawerForm), generic admin nouns. The most sensitive screen (DocumentActionModal) is hardcoded Spanish violating the repo's own i18n rule. | Cheap to fix: i18n infrastructure is mature (POS ~4,100-line `LanguageContext.tsx`; landing per-entity `{es,en}` JSON; dashboard 840+ keys) — renaming "Clientes"→"Aliados/Socios" and CTAs is a translation-file change, not an architecture change. |

---

## 2. Which principles live in the mechanics (not just copy)

Only three principles have any real embodiment in code, and each is qualified:

1. **Local development (#9) — the platform's authentic SSE asset.** The mature pillar of the ecosystem is *exactly* the thing a Costa Rican micro-seller cannot get from Shopify: legally valid Hacienda v4.4 e-invoicing (jbiller pipeline), CR fiscal catalogs, SINPE, WhatsApp-first selling, and offline-first POS for unreliable connectivity. This is genuine "tecnología al servicio de las personas" — but it is framed nowhere in the landing, which instead markets the unbuilt barter/fairs layer. The strongest implemented capability and the strongest marketed capability are disjoint sets.
2. **Intra-org collaboration (#11).** Memberships, invitations, assignments, and cash-closing reconciliation are real multi-person mechanics — though degraded by broken journeys (cajero dead-end, unmounted ClosingFlow, lost invitation redirects) and unenforced roles.
3. **Free access (#8).** True today because no billing code exists at all — a fact, not a designed commitment.

**Conversely, two principles are actively contradicted by mechanics:** transparency (#7: fake contact form, simulated publish success, placeholder traction claims) and fairness-by-system-rules (#6: ecosystem-wide IDOR, unmounted RBAC, plaintext fiscal credentials). For a brand whose three trust pills are *Transparencia / Comercio justo / Comunidad local*, the implementation currently falsifies the first two.

## 3. Which principles are landing rhetoric only

The entire community/SSE differentiator layer: **barter (#1), fairs (#2), cross-org cooperation (#3), mutual support networks (#4), community governance (#5), conscious consumption (#10), women-led empowerment (#12), cultural preservation (#13), economy-as-healing (#14)**. None has a single entity, endpoint, or UI in any of the seven systems (verified Phase 4 + fresh greps). The platform behind the SSE landing is, mechanically, (a) a CR fiscal/POS system that works and (b) a storefront-builder whose commerce loop is broken (org-mode product endpoints missing, checkout persists nothing). There is no buyer identity at all — Cognito users are org operators — so every "community" principle lacks even its actor.

## 4. Concrete product mechanisms per unsupported principle

Ordered by leverage (trust repair → enforcement → community domain), with reuse notes:

| Principle | Proposed mechanism | Reuses / depends on |
|---|---|---|
| Transparency (#7) | Wire the contact form to SES (the stack already sends SES mail: `InvitationService.ts`, cross-app-be `email_service.py`); make `DeploymentService` report real pipeline status; replace placeholder traction/testimonials with real or none; implement the claimed cookie banner or delete the claim; publish an honest "what's live vs roadmap" page (the present-tense barter/fairs copy is a legal/trust liability). | Existing SES + SNS infra; content-only edits in `landing-client/src/content/*`. |
| Fairness by system rules (#6) | Mount the already-built enforcement: markets-api `permissions.ts` middleware + populate `req.userId`; replicate cross-app-be's correct gateway pattern (`integration.request.header.x-user-id: context.authorizer.claims.sub` in its `api-gateway/template.yml`) to markets-api and sales-api; finish the lambda-authorizer membership TODO (`organization_auth_checker.py:73-84`); encrypt P12/PIN/ATV secrets (KMS/Secrets Manager). | ~80% already written; this is wiring, not building. Prerequisite for *every* community feature — communities cannot run on a platform with cross-tenant credential exposure. |
| Free / fair fees (#8) | Publish a solidarity pricing model before monetizing: e.g., free tier permanent for micro-sellers, capped transparent commission, cooperative discount, optional solidarity contribution ("aporte") instead of forced fees. `plan` field and Stripe scaffold exist as the insertion point. | `Organization.plan`, `stripeCustomerId`, `PaymentSettings` — schema exists, SDK absent. |
| Local discovery / fairs (#2, #3, #9) | Step 1: fix the broken loop — add `/products` and `/categories` to `PublicOrgController.ts` (storefronts already call them). Step 2: a `marketplace`/discovery read-model aggregating products across orgs by canton/district (CR geo data already in data-api + storefront `@/data/locations`). Step 3: a fair domain — `fairs`, `fair_participations` (org applies/approved), time-boxed aggregated catalog page per fair, virtual + physical types per `fairs.json`. | PublicOrgController, CR geographic hierarchy, template SPA infrastructure, SNS event mesh for fair lifecycle events. |
| Barter (#1) | An `exchange_offers` entity (org or member offers product/service/skill, wants X), matching/browse UI, `exchange_agreements` record (both parties confirm), optionally a mutual-credit ledger later. Keep platform as facilitator (consistent with `terms.json` §4) — record agreements, don't settle them. | Needs buyer/member identity first (see below); product model reusable for offered goods. |
| Community membership & mutual aid (#4) | Introduce a **buyer/community member identity** (the missing actor): lightweight Cognito profile not tied to org operation; then a directory of stores/people with skills tags, follow/contact, and mentor matching. | Cognito pool + markets-api user sync already exist; `customer` default role is already in the model (currently meaningless). |
| Community governance (#5) | A `cooperative` organization type: multiple owners or weighted memberships in `OrganizationMember`, proposal + vote entities for org decisions (pricing, members, fair participation), and visible role assignment backed by the *enforced* RBAC of #6. | `OrganizationMember.roleId` + seeded RBAC model is the substrate; enforcement (#6) is the prerequisite. |
| Cooperative revenue / collective value (#3) | "Canastas comunitarias": a bundle entity referencing products from ≥2 organizations with per-org revenue attribution; seller profile section listing collaborations. Requires marketplace read-model + consumer order domain (which also fixes the WhatsApp-only checkout having no order record). | Depends on local-discovery step 2 and a consumer `orders` domain (currently nonexistent — cross-app-be `/orders` is B2B cross-docking). |
| Crowdfunding (Jala de Piedra) (#4) | A `campaigns` + `contributions` domain for cooperative capital needs, with the research's stone-pulling progress visualization; payment via SINPE reference (already the POS pattern) before any gateway integration. | jbiller SNS/SQS FIFO pipeline pattern; SINPE reference-payment convention. |
| Conscious consumption / provenance (#10, #13) | Provenance/story fields on products (producer bio, origin, practices) + a "story" section type in the CMS component catalog (`entities/Component.ts`, `component-seed.ts`) rendered inline in storefront product pages — the research's "education woven into commerce". | Existing CMS Page/Section/Content + component catalog; storefront templates. |
| Women-led / cooperative visibility (#12) | Org-profile attributes (`womenLed`, `cooperative`, `indigenousLed` booleans/badges) surfaced in discovery filters and fair listings; curation handled by the same admin roles. | One Drizzle migration on `organizations` + UI badges. |
| Solidarity language (#15) | Rename across translation layers: POS `LanguageContext.tsx`, landing content JSON, storefront copy — "Clientes"→community-framed term, transactional CTAs→support-framed CTAs; fix DocumentActionModal hardcoded Spanish while touching it. | Pure i18n work; zero schema change; highest symbolism-per-effort ratio. |

### Sequencing logic

1. **Stop contradicting the principles** (transparency fixes + secrets encryption + authorization mounting). A solidarity brand running on IDOR-open infrastructure with plaintext taxpayer certificates is the inverse of its promise; this is also the cheapest phase since most enforcement code exists unmounted.
2. **Make the implemented strength the story**: reposition the landing around what is genuinely solidaristic and real today — free CR-compliant invoicing, WhatsApp/offline-first selling for low-connectivity micro-sellers — and move barter/fairs copy to explicit roadmap framing.
3. **Build the community domain in dependency order**: public catalog fix → buyer identity → local discovery → fairs → barter/bundles/crowdfunding/governance. Each later mechanism depends on actors and surfaces created by the earlier ones.

---

## 5. Verdict

The Tsuru/JMarkets ecosystem today is a **fiscally excellent, socially empty** platform: of ~15 SSE principles claimed across landing and research, only *local fit* (CR fiscal/channel localization), *intra-org collaboration* (memberships/invitations), and *free access* (by absence of billing) have any mechanical embodiment — while the two most-repeated trust values (*transparencia*, *comercio justo*) are actively contradicted by fake flows and missing authorization. Every community-shaped differentiator (trueque, ferias, redes de apoyo, governance, conscious consumption, cultural preservation) is present-tense marketing with zero code behind it, and lacks even the buyer/community-member actor needed to build it. The good news is asymmetric: the contradiction fixes are mostly wiring of already-written code, the i18n/copy mechanisms are trivial, and the platform's truly differentiated asset — free legal e-invoicing for the smallest sellers — is itself an unclaimed solidarity story.
