node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
*.env
logs/

*.production
lambda-package.zip

# Repo split (DONE 2026-06-12 — folders untracked): each lives in its own repo
# with its own GH Actions deploy. Local folders are standalone working copies.
# Never `git add -f` these back into the monorepo.
# fe/ — frontend grouping (mirrors be/); relocated under fe/ 2026-06-12.
# fe/pos-system → https://github.com/chepelcr/tsuru-pos-system
fe/pos-system/

# fe/landing → https://github.com/chepelcr/tsuru-landing
fe/landing/

# fe/storefront-sdk → https://github.com/chepelcr/tsuru-storefront-sdk
# Shared storefront data-layer package (published to GitHub Packages, consumed by the
# templates). Own repo; local folder is a standalone working copy.
fe/storefront-sdk/

# NOTE: fe/dashboard is NOT ignored — it stays tracked in the monorepo (being retired, TSR-091).

# be/ — each subfolder is its own standalone git repo
be/management-be/
be/store-be/
be/sales-be/
be/data-be/

# Infrastructure — own git repo
Infrastructure/

# ── Split out to their own public repos (GH-Pages examples at *.examples.tsuru.jcampos.dev) ──
# Each storefront template now lives in chepelcr/template-<name>; files remain tracked here
# for now (provisioner clones the repos). New work belongs in the standalone repos.
templates/artisan-crafts/
templates/beauty-essentials/
templates/fitness-hub/
templates/gourmet-foods/
templates/jmarkets-demo/
templates/pet-care/
templates/tech-gadgets/
templates/vintage-fashion/
