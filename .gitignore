node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
*.env
logs/

*.production
lambda-package.zip

# Repo split (DONE 2026-06-12 — folders untracked): each lives in its own repo
# with its own GH Actions deploy. Local folders are standalone working copies.
# Never `git add -f` these back into the monorepo.
# templates/pos-system → https://github.com/chepelcr/tsuru-pos-system
templates/pos-system/

# landing-client → https://github.com/chepelcr/tsuru-landing
landing-client/

# server → https://github.com/chepelcr/tsuru-platform-api (private)
server/
