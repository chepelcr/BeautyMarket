AWSTemplateFormatVersion: "2010-09-09"
Description: JMarkets Lambda Function (Simple CloudFormation - No SAM)

Parameters:
  Environment:
    Type: String
    Default: dev
    AllowedValues: [dev, staging, prod]
    Description: Environment name — used to import shared infra policy and Cognito exports

  FunctionNameParam:
    Description: "Function Name (pattern: jmarkets-{env}-api-handler)"
    Type: String
    Default: "jmarkets-dev-api-handler"

  DatabaseURLParam:
    Description: Database URL
    Type: String
    NoEcho: true

Resources:
  LambdaFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Ref FunctionNameParam
      Description: "JMarkets API Handler"
      PackageType: Zip
      Runtime: nodejs20.x
      Handler: lambda.handler
      Role: !GetAtt LambdaRole.Arn
      Code:
        ZipFile: |
          exports.handler = async (event) => {
            return {
              statusCode: 200,
              body: JSON.stringify({ message: 'Placeholder - Deploy code with update-lambda.sh' })
            };
          };
      MemorySize: 512
      Timeout: 30
      Environment:
        Variables:
          NODE_ENV: production
          NEW_DATABASE_URL: !Ref DatabaseURLParam
          AWS_COGNITO_USER_POOL_ID:
            Fn::ImportValue: "jmarkets-cognito-UserPoolId"
          AWS_COGNITO_CLIENT_ID:
            Fn::ImportValue: "jmarkets-cognito-UserPoolClientId"
          ORGANIZATION_TOPIC_ARN:
            Fn::ImportValue: !Sub "jmarkets-${Environment}-organization-events-topic-arn"

  LambdaRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "jmarkets-${Environment}-lambda-role"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - lambda.amazonaws.com
            Action:
              - sts:AssumeRole
      Path: /
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
        - Fn::ImportValue: !Sub "jcampos-${Environment}-cognito-policy-arn"
        - Fn::ImportValue: !Sub "jcampos-${Environment}-s3-policy-arn"
        - Fn::ImportValue: !Sub "jcampos-${Environment}-ses-policy-arn"
        - Fn::ImportValue: !Sub "jcampos-${Environment}-sns-policy-arn"
        - Fn::ImportValue: !Sub "jcampos-${Environment}-sqs-policy-arn"

  LambdaLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub "/aws/lambda/${FunctionNameParam}"
      RetentionInDays: 7

Outputs:
  LambdaFunctionArn:
    Description: Lambda Function ARN
    Value: !GetAtt LambdaFunction.Arn
    Export:
      Name: !Sub "jmarkets-${Environment}-lambda-arn"

  LambdaFunctionName:
    Description: Lambda Function Name
    Value: !Ref LambdaFunction
    Export:
      Name: !Sub "jmarkets-${Environment}-lambda-name"
